name: test-foos3
source: test-local-postgres
type: raw_sql
rawSql: >-
  SELECT generate_series(0,2000) AS id, md5(random()::text) AS descr,
  md5(random()::text) AS foo
isSchema: false
primaryKey: id
